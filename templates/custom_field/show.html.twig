{% extends 'base.html.twig' %}

{% block title %}CustomField{% endblock %}

{% block body %}
    <h1>CustomField</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ custom_field.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ custom_field.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ custom_field.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_custom_field_index') }}">back to list</a>

    <a href="{{ path('app_custom_field_edit', {'id': custom_field.id}) }}">edit</a>

    {{ include('custom_field/_delete_form.html.twig') }}
{% endblock %}
