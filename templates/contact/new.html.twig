{% extends 'base.html.twig' %}

{% block title %}New Contact{% endblock %}

{% block body %}
    <div class="bg-white shadow-md rounded-lg p-6 max-w-3xl mx-auto">
        <h1 class="text-2xl font-bold text-gray-700 mb-6">Create New Contact</h1>

        <!-- Form -->
        <div class="space-y-4">
            {{ include('contact/_form.html.twig') }}
        </div>

        <!-- Back to List Button -->
        <div class="mt-6">
            <a href="{{ path('app_contact_index') }}" class="bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded shadow">
                Back to List
            </a>
        </div>
    </div>
{% endblock %}
